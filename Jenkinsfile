pipeline {
  agent any
  stages {
    stage('') {
      steps {
        git(url: 'https://github.com/republicprotocol/renex-swapper-go.git', branch: 'master')
        sh 'echo "Hello World"'
      }
    }
  }
}